# AI 解答功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `questions` 表的题目离线预生成难度自适应的 AI Markdown 解答，前端默认折叠、可逐条或一键展开。

**Architecture:** 仿照现有 `aggregator` 离线模块——新增 `answerer` 模块逐题调用 DeepSeek（每题独立，难度靠 prompt 自判断），结果写入 `questions.answer_ai` 列并用 Redis 按题文去重缓存。API 两个现有路由各加一列返回，前端共用一个 `AnswerBlock` 折叠组件接入摘要页清单与搜索卡片。

**Tech Stack:** Python (SQLModel async, openai SDK, Redis), FastAPI, Typer CLI, Next.js 15 + react-markdown + Tailwind, pytest-asyncio。

## Global Constraints

- 不动现有 `answer_brief` 列（原帖答案要点），AI 答案存独立的 `answer_ai` 列。
- 每题**独立**调用 DeepSeek，禁止批量总结。
- 难度自适应完全靠 prompt，不做难度预分类。
- AI 答案为 Markdown，前端用 `react-markdown` + `remark-gfm` 渲染，复用 `.prose-il` 样式。
- 仅给所属帖 `quality_score >= min_quality`（默认 30）且 `extract_status='done'` 的题生成。
- 复用 `llm/cache.py` 的 `make_cache_key` / `cache_get` / `cache_set`，namespace 用 `"answer"`。
- 不新增 API 端点；不引入 Celery，CLI 同步跑。
- 提交信息英文，结尾加 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

---

### Task 1: 数据库列 + 配置 + ORM 字段

**Files:**
- Create: `sql/002_answer_ai.sql`
- Modify: `src/interviewlens/db/models.py:59-74` (Question 类)
- Modify: `src/interviewlens/config.py` (加 `answer_prompt_version`)

**Interfaces:**
- Produces: `Question.answer_ai: str | None`、`Question.answer_ai_version: int`；`settings.answer_prompt_version: int`。

- [ ] **Step 1: 写 SQL 迁移文件**

`sql/002_answer_ai.sql`:
```sql
-- AI-generated answers for individual questions (see answerer module).
ALTER TABLE questions ADD COLUMN IF NOT EXISTS answer_ai TEXT;
ALTER TABLE questions ADD COLUMN IF NOT EXISTS answer_ai_version INT NOT NULL DEFAULT 0;
```

- [ ] **Step 2: 应用迁移到本地库**

Run: `docker compose exec -T postgres psql -U postgres -d interviewlens -f - < sql/002_answer_ai.sql`
（若用户名/库名不同，按 `.env` 的 `DATABASE_URL` 调整。）
Expected: `ALTER TABLE` ×2，无报错。

- [ ] **Step 3: 给 Question ORM 加字段**

`src/interviewlens/db/models.py`，在 `answer_brief: str | None = None` 之后加：
```python
    answer_ai: str | None = None
    answer_ai_version: int = Field(default=0)
```

- [ ] **Step 4: 给 config 加版本字段**

`src/interviewlens/config.py`，在 `extract_prompt_version: int = 1` 附近加：
```python
    answer_prompt_version: int = 1
```

- [ ] **Step 5: 验证导入无误**

Run: `uv run python -c "from interviewlens.db import Question; from interviewlens.config import settings; print(Question.model_fields['answer_ai'], settings.answer_prompt_version)"`
Expected: 打印字段信息和 `1`，无异常。

- [ ] **Step 6: Commit**

```bash
git add sql/002_answer_ai.sql src/interviewlens/db/models.py src/interviewlens/config.py
git commit -m "feat(db): add answer_ai columns + answer_prompt_version config

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Answerer prompt

**Files:**
- Modify: `src/interviewlens/llm/prompts.py` (文件末尾追加)
- Test: `tests/test_answerer_prompt.py`

**Interfaces:**
- Produces: `ANSWERER_SYSTEM: str`；`build_answerer_messages(*, content: str, category: str | None) -> list[dict]`。

- [ ] **Step 1: 写失败测试**

`tests/test_answerer_prompt.py`:
```python
"""Answerer prompt builder shape."""
from __future__ import annotations

from interviewlens.llm.prompts import ANSWERER_SYSTEM, build_answerer_messages


def test_messages_have_system_and_user() -> None:
    msgs = build_answerer_messages(content="什么是 Redis 分布式锁？", category="后端")
    assert len(msgs) == 2
    assert msgs[0]["role"] == "system"
    assert msgs[0]["content"] == ANSWERER_SYSTEM
    assert msgs[1]["role"] == "user"
    assert "Redis 分布式锁" in msgs[1]["content"]


def test_category_optional() -> None:
    msgs = build_answerer_messages(content="讲讲 TCP", category=None)
    assert "讲讲 TCP" in msgs[1]["content"]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_answerer_prompt.py -v`
Expected: FAIL，ImportError（ANSWERER_SYSTEM 未定义）。

- [ ] **Step 3: 实现 prompt**

`src/interviewlens/llm/prompts.py` 末尾追加：
```python
# ----------------------------------------------------------------- Answerer

ANSWERER_SYSTEM = """你是资深技术面试官，为面经题目撰写参考答案，帮助求职者备考。

铁律：
1. 难度自适应：简单概念题用 2-3 句直接讲透，不堆字凑长度；复杂题再展开，
   可分点、用代码块或对比，但每一点都要有信息量，不灌水。
2. 输出纯 Markdown（可用标题、列表、代码块、**强调**），不要包在代码围栏里。
3. 讲清原理与考察点，面向面试场景；不要「多刷题」「夯实基础」这类空话。
4. 题目信息不足或超出常规知识范围无法作答时，直说「题目信息不足」并给出
   可能的理解方向，绝不编造事实。"""


def build_answerer_messages(*, content: str, category: str | None) -> list[dict]:
    """Compose chat messages for the Answerer (one question per call)."""
    cat_hint = f"（分类：{category}）" if category else ""
    user = f"请为下面这道面试题写参考答案{cat_hint}：\n\n{content}"
    return [
        {"role": "system", "content": ANSWERER_SYSTEM},
        {"role": "user", "content": user},
    ]
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_answerer_prompt.py -v`
Expected: 2 passed。

- [ ] **Step 5: Commit**

```bash
git add src/interviewlens/llm/prompts.py tests/test_answerer_prompt.py
git commit -m "feat(llm): add answerer system prompt + message builder

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Answerer 核心模块

**Files:**
- Create: `src/interviewlens/answerer/__init__.py`
- Create: `src/interviewlens/answerer/answerer.py`
- Test: `tests/test_answerer.py`

**Interfaces:**
- Consumes: `build_answerer_messages` (Task 2)、`settings.answer_prompt_version` (Task 1)、`make_cache_key/cache_get/cache_set`、`session_scope`、`get_client`、`incr_tokens`。
- Produces:
  - `AnswerOutcome` dataclass: `generated: int, cache_hits: int, skipped: int, failed: int`。
  - `async def generate_one(*, content: str, category: str | None, trace=None) -> str | None` — 单题生成（含缓存），失败返回 None。
  - `async def run_answers(*, company: str | None = None, position: str | None = None, limit: int | None = None, min_quality: int = 30, regenerate: bool = False) -> AnswerOutcome`。

- [ ] **Step 1: 写失败测试**

`tests/test_answerer.py`:
```python
"""Answerer unit tests — cache + per-question generation, no real LLM/DB."""
from __future__ import annotations

import pytest

import interviewlens.answerer.answerer as ans


@pytest.mark.asyncio
async def test_generate_one_uses_cache(monkeypatch) -> None:
    # cache_get returns a hit → no LLM call
    async def fake_get(key):
        return {"answer": "缓存答案"}

    called = {"llm": False}

    def fake_client():
        called["llm"] = True
        raise AssertionError("LLM should not be called on cache hit")

    monkeypatch.setattr(ans, "cache_get", fake_get)
    monkeypatch.setattr(ans, "get_client", fake_client)

    out = await ans.generate_one(content="什么是 GIL？", category="Python")
    assert out == "缓存答案"
    assert called["llm"] is False


@pytest.mark.asyncio
async def test_generate_one_calls_llm_on_miss(monkeypatch) -> None:
    async def fake_get(key):
        return None

    sets = {}

    async def fake_set(key, value, **kw):
        sets[key] = value

    class _Msg:
        content = "生成的答案"

    class _Choice:
        message = _Msg()

    class _Usage:
        def model_dump(self):
            return {"prompt_tokens": 10, "completion_tokens": 20}

    class _Resp:
        choices = [_Choice()]
        usage = _Usage()

    class _Completions:
        async def create(self, **kw):
            return _Resp()

    class _Chat:
        completions = _Completions()

    class _Client:
        chat = _Chat()

    async def fake_incr(**kw):
        return None

    monkeypatch.setattr(ans, "cache_get", fake_get)
    monkeypatch.setattr(ans, "cache_set", fake_set)
    monkeypatch.setattr(ans, "get_client", lambda: _Client())
    monkeypatch.setattr(ans, "incr_tokens", fake_incr)

    out = await ans.generate_one(content="什么是 GIL？", category="Python")
    assert out == "生成的答案"
    assert len(sets) == 1  # result was cached
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_answerer.py -v`
Expected: FAIL，ModuleNotFoundError（answerer 不存在）。

- [ ] **Step 3: 实现模块**

`src/interviewlens/answerer/__init__.py`:
```python
"""Offline AI answer generation for individual questions."""
from .answerer import AnswerOutcome, generate_one, run_answers

__all__ = ["AnswerOutcome", "generate_one", "run_answers"]
```

`src/interviewlens/answerer/answerer.py`:
```python
"""Answerer: per-question AI answer generation (offline).

For each eligible question, call DeepSeek once (never batched) so each answer
gets full attention. Length adapts to difficulty via the prompt. Results are
cached in Redis by question text + prompt version, then written to
questions.answer_ai.
"""
from __future__ import annotations

import asyncio
from dataclasses import dataclass

from sqlalchemy import text as sa_text

from ..config import settings
from ..db import session_scope
from ..llm.cache import cache_get, cache_set, make_cache_key
from ..llm.deepseek import get_client
from ..llm.prompts import build_answerer_messages
from ..logging import log
from ..observability import incr_tokens

ANSWER_NAMESPACE = "answer"
CONCURRENCY = 5
MAX_TOKENS = 1500


@dataclass(slots=True)
class AnswerOutcome:
    generated: int = 0
    cache_hits: int = 0
    skipped: int = 0
    failed: int = 0


async def generate_one(
    *, content: str, category: str | None, trace=None
) -> str | None:
    """Generate (or fetch cached) AI answer for one question. None on failure."""
    key = make_cache_key(
        namespace=ANSWER_NAMESPACE,
        payload={"content": content},
        version=settings.answer_prompt_version,
    )
    cached = await cache_get(key)
    if cached and cached.get("answer"):
        return cached["answer"]

    messages = build_answerer_messages(content=content, category=category)
    try:
        client = get_client()
        resp = await client.chat.completions.create(
            model=settings.deepseek_model_chat,
            messages=messages,  # type: ignore[arg-type]
            temperature=0.3,
            max_tokens=MAX_TOKENS,
        )
    except Exception as exc:  # noqa: BLE001
        log.error("answer.llm_failed", err=str(exc))
        return None

    answer = (resp.choices[0].message.content or "").strip()
    usage = resp.usage.model_dump() if resp.usage else None
    if usage:
        await incr_tokens(
            prompt=int(usage.get("prompt_tokens") or 0),
            completion=int(usage.get("completion_tokens") or 0),
        )
    if trace is not None:
        try:
            trace.generation(
                name="answerer.answer",
                model=settings.deepseek_model_chat,
                input=messages,
                output=answer,
                usage=usage,
            ).end()
        except Exception:  # noqa: BLE001
            pass

    if answer:
        await cache_set(key, {"answer": answer})
    return answer or None


async def _fetch_targets(
    *,
    company: str | None,
    position: str | None,
    limit: int | None,
    min_quality: int,
    regenerate: bool,
) -> list[dict]:
    """Select questions needing an AI answer."""
    clauses = [
        "po.extract_status = 'done'",
        "COALESCE(po.quality_score, 0) >= :min_q",
    ]
    params: dict[str, object] = {"min_q": min_quality, "ver": settings.answer_prompt_version}
    if not regenerate:
        clauses.append("(q.answer_ai IS NULL OR q.answer_ai_version < :ver)")
    joins = "JOIN posts po ON po.id = q.post_id"
    if company:
        joins += (
            " JOIN post_company_position pcp_c ON pcp_c.post_id = po.id"
            " JOIN companies c ON c.id = pcp_c.company_id"
        )
        clauses.append("c.canonical = :company")
        params["company"] = company
    if position:
        joins += (
            " JOIN post_company_position pcp_p ON pcp_p.post_id = po.id"
            " JOIN positions p ON p.id = pcp_p.position_id"
        )
        clauses.append("p.canonical = :position")
        params["position"] = position

    limit_sql = ""
    if limit is not None:
        limit_sql = "LIMIT :limit"
        params["limit"] = limit

    sql = f"""
    SELECT DISTINCT q.id, q.content, q.category
    FROM questions q
    {joins}
    WHERE {' AND '.join(clauses)}
    ORDER BY q.id
    {limit_sql}
    """
    async with session_scope() as session:
        rows = (await session.execute(sa_text(sql), params)).all()
    return [{"id": r[0], "content": r[1], "category": r[2]} for r in rows]


async def _write_answer(qid: int, answer: str) -> None:
    async with session_scope() as session:
        await session.execute(
            sa_text(
                "UPDATE questions SET answer_ai = :a, answer_ai_version = :v WHERE id = :id"
            ),
            {"a": answer, "v": settings.answer_prompt_version, "id": qid},
        )


async def run_answers(
    *,
    company: str | None = None,
    position: str | None = None,
    limit: int | None = None,
    min_quality: int = 30,
    regenerate: bool = False,
) -> AnswerOutcome:
    """Generate AI answers for all eligible questions."""
    targets = await _fetch_targets(
        company=company,
        position=position,
        limit=limit,
        min_quality=min_quality,
        regenerate=regenerate,
    )
    log.info("answer.start", targets=len(targets))
    outcome = AnswerOutcome()
    sem = asyncio.Semaphore(CONCURRENCY)

    async def _one(t: dict) -> None:
        async with sem:
            answer = await generate_one(content=t["content"], category=t["category"])
            if answer is None:
                outcome.failed += 1
                return
            await _write_answer(t["id"], answer)
            outcome.generated += 1

    await asyncio.gather(*(_one(t) for t in targets))
    return outcome
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_answerer.py -v`
Expected: 2 passed。

- [ ] **Step 5: 运行 lint**

Run: `uv run ruff check src/interviewlens/answerer/ tests/test_answerer.py`
Expected: All checks passed。

- [ ] **Step 6: Commit**

```bash
git add src/interviewlens/answerer/ tests/test_answerer.py
git commit -m "feat(answerer): per-question AI answer generation with cache

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: CLI `il answer` 命令

**Files:**
- Modify: `src/interviewlens/cli.py` (新增一个 `@app.command()`)

**Interfaces:**
- Consumes: `run_answers`、`AnswerOutcome` (Task 3)。

- [ ] **Step 1: 加命令**

`src/interviewlens/cli.py`，在 `aggregate` 命令之后追加（沿用文件已有的 `typer`、`console`、`Table`、`asyncio` 导入）：
```python
@app.command()
def answer(
    company: str | None = typer.Option(None, help="Canonical company name; omit for all"),
    position: str | None = typer.Option(None, help="Canonical position name"),
    limit: int | None = typer.Option(None, help="Max questions this run"),
    min_quality: int = typer.Option(30),
    regenerate: bool = typer.Option(False, "--regenerate", help="Ignore existing answers/version"),
) -> None:
    """Generate difficulty-adaptive AI answers for individual questions (offline)."""
    from .answerer import run_answers

    async def _run() -> None:
        outcome = await run_answers(
            company=company,
            position=position,
            limit=limit,
            min_quality=min_quality,
            regenerate=regenerate,
        )
        table = Table(title="answer", show_header=False)
        table.add_column("k", style="cyan")
        table.add_column("v")
        table.add_row("generated", str(outcome.generated))
        table.add_row("cache_hits", str(outcome.cache_hits))
        table.add_row("skipped", str(outcome.skipped))
        table.add_row("failed", str(outcome.failed))
        console.print(table)

    asyncio.run(_run())
```

- [ ] **Step 2: 验证命令注册**

Run: `uv run il answer --help`
Expected: 显示 `--company --position --limit --min-quality --regenerate` 选项，无异常。

- [ ] **Step 3: Commit**

```bash
git add src/interviewlens/cli.py
git commit -m "feat(cli): add 'il answer' command

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: API — 返回 answer_ai

**Files:**
- Modify: `src/interviewlens/api/schemas.py:33-44` (QuestionOut)
- Modify: `src/interviewlens/api/routes_summary.py:94-137` (list_raw_questions)
- Modify: `src/interviewlens/api/routes_search.py:61-72` (search SQL)

**Interfaces:**
- Produces: `QuestionOut.answer_ai: str | None`，两个路由 JSON 中含该字段。

- [ ] **Step 1: schema 加字段**

`src/interviewlens/api/schemas.py`，`QuestionOut` 里 `answer_brief` 之后加：
```python
    answer_ai: str | None = None
```

- [ ] **Step 2: summary 路由补字段**

`src/interviewlens/api/routes_summary.py`：
SQL 的 SELECT 把 `q.answer_brief,` 改为 `q.answer_brief, q.answer_ai,`，且 `po.quality_score` 等后续列顺序对应。即改为：
```python
        SELECT q.id, q.post_id, q.round_no, q.round_type,
               q.content, q.category, q.answer_brief, q.answer_ai,
               COALESCE(po.quality_score, 0) AS quality_score,
               po.source_url
```
并在返回的 `QuestionOut(...)` 构造里，把 `answer_brief=r[6],` 之后的索引顺延：
```python
        QuestionOut(
            id=r[0],
            post_id=r[1],
            round_no=r[2],
            round_type=r[3],
            content=r[4],
            category=r[5],
            answer_brief=r[6],
            answer_ai=r[7],
            quality_score=r[8],
            source_url=r[9],
        )
```

- [ ] **Step 3: search 路由补字段**

`src/interviewlens/api/routes_search.py` 的 SQL SELECT，把 `q.answer_brief,` 改为 `q.answer_brief, q.answer_ai,`：
```python
    SELECT q.id, q.post_id, q.round_no, q.round_type, q.content, q.category,
           q.answer_brief, q.answer_ai, po.quality_score, po.source_url,
           1 - (q.embedding <=> CAST(:vec AS vector)) AS similarity
```
（该路由用 `QuestionOut(**dict(r))` 按列名自动映射，无需改构造代码。）

- [ ] **Step 4: 运行现有 API 测试**

Run: `uv run pytest tests/test_api.py -v`
Expected: 全部 passed（新增可选字段不破坏现有断言）。

- [ ] **Step 5: 运行全套测试 + lint**

Run: `uv run pytest && uv run ruff check src/`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add src/interviewlens/api/schemas.py src/interviewlens/api/routes_summary.py src/interviewlens/api/routes_search.py
git commit -m "feat(api): expose answer_ai in question responses

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 前端 — AnswerBlock 组件 + Question 类型

**Files:**
- Create: `web/src/components/answer-block.tsx`
- Modify: `web/src/lib/api.ts:33-44` (Question type)

**Interfaces:**
- Produces: `<AnswerBlock answer={string | null} expandAll?={boolean} />`。`answer` 为 null 时渲染 null（不显示入口）；`expandAll` 变化时覆盖内部展开状态。

- [ ] **Step 1: Question 类型加字段**

`web/src/lib/api.ts`，`Question` type 里 `answer_brief` 之后加：
```typescript
  answer_ai: string | null;
```

- [ ] **Step 2: 写 AnswerBlock 组件**

`web/src/components/answer-block.tsx`:
```tsx
"use client";

import { useState, useEffect } from "react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import { ChevronDown, Sparkles } from "lucide-react";
import { cn } from "@/lib/cn";

type Props = {
  answer: string | null;
  /** Controlled override from a parent "expand all" toggle. */
  expandAll?: boolean;
};

export function AnswerBlock({ answer, expandAll }: Props) {
  const [open, setOpen] = useState(false);

  // Parent "expand all" toggle overrides local state when it changes.
  useEffect(() => {
    if (expandAll !== undefined) setOpen(expandAll);
  }, [expandAll]);

  if (!answer) return null;

  return (
    <div className="mt-2">
      <button
        onClick={() => setOpen((v) => !v)}
        className="inline-flex items-center gap-1 font-mono text-[10px] uppercase tracking-widest text-accent-ink transition hover:underline"
      >
        <Sparkles className="h-3 w-3" />
        AI 解答
        <ChevronDown className={cn("h-3 w-3 transition", open && "rotate-180")} />
      </button>
      {open && (
        <div className="prose-il mt-2 max-w-[68ch] border-l-2 border-accent/30 pl-3">
          <ReactMarkdown remarkPlugins={[remarkGfm]}>{answer}</ReactMarkdown>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: typecheck**

Run: `cd web && pnpm typecheck`
Expected: 无类型错误。

- [ ] **Step 4: Commit**

```bash
git add web/src/components/answer-block.tsx web/src/lib/api.ts
git commit -m "feat(web): AnswerBlock collapsible component + Question.answer_ai

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 前端 — 摘要页清单接入

**Files:**
- Modify: `web/src/components/summary-view.tsx:116-158` (RawQuestionList)

**Interfaces:**
- Consumes: `<AnswerBlock>` (Task 6)。

- [ ] **Step 1: 引入组件 + 展开全部状态**

`web/src/components/summary-view.tsx` 顶部 import 区加：
```tsx
import { AnswerBlock } from "@/components/answer-block";
```

把 `RawQuestionList` 改为带「展开所有答案」按钮和 `expandAll` 状态，并在每条题目里渲染 `AnswerBlock`：
```tsx
function RawQuestionList({ questions }: { questions: Question[] }) {
  const [expandAll, setExpandAll] = useState<boolean | undefined>(undefined);
  const hasAnswers = questions.some((q) => q.answer_ai);

  const grouped: Record<string, Question[]> = {};
  for (const q of questions) {
    const cat = q.category || "未分类";
    if (!grouped[cat]) grouped[cat] = [];
    grouped[cat].push(q);
  }

  return (
    <div className="mt-4 space-y-6">
      {hasAnswers && (
        <button
          onClick={() => setExpandAll((v) => !v)}
          className="font-mono text-[10px] uppercase tracking-widest text-accent-ink hover:underline"
        >
          {expandAll ? "收起所有答案" : "展开所有答案"}
        </button>
      )}
      {Object.entries(grouped).map(([cat, qs]) => (
        <section key={cat}>
          <h3 className="font-mono text-[10px] uppercase tracking-[0.18em] text-muted/60 mb-3">
            {cat} <span className="text-muted/30">· {qs.length}</span>
          </h3>
          <ol className="space-y-3 list-decimal list-inside">
            {qs.map((q) => (
              <li key={q.id} className="pl-1">
                <p className="inline text-sm text-ink/90 leading-relaxed">{q.content}</p>
                {q.answer_brief && (
                  <p className="mt-1 ml-0 pl-5 border-l-2 border-accent/20 text-xs text-muted/70 leading-relaxed">
                    {q.answer_brief}
                  </p>
                )}
                <AnswerBlock answer={q.answer_ai} expandAll={expandAll} />
                {q.source_url && (
                  <a
                    href={q.source_url}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="ml-2 font-mono text-[10px] text-muted/30 hover:text-accent-ink underline underline-offset-2"
                  >
                    来源 →
                  </a>
                )}
              </li>
            ))}
          </ol>
        </section>
      ))}
    </div>
  );
}
```
（`useState` 已在该文件顶部从 react 导入，无需重复。）

- [ ] **Step 2: typecheck + lint**

Run: `cd web && pnpm typecheck && pnpm lint`
Expected: 无错误。

- [ ] **Step 3: Commit**

```bash
git add web/src/components/summary-view.tsx
git commit -m "feat(web): AI answers in summary question list with expand-all

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 前端 — 搜索结果卡片接入

**Files:**
- Modify: `web/src/components/question-card.tsx` (加 AnswerBlock + expandAll prop)
- Modify: `web/src/app/search/page.tsx` (列表顶部加展开全部按钮)

**Interfaces:**
- Consumes: `<AnswerBlock>` (Task 6)。
- Produces: `QuestionCard` 新增可选 prop `expandAll?: boolean`。

- [ ] **Step 1: QuestionCard 接入 AnswerBlock**

`web/src/components/question-card.tsx`：
import 区加：
```tsx
import { AnswerBlock } from "@/components/answer-block";
```
`Props` type 加 `expandAll?: boolean;`，函数签名改为 `export function QuestionCard({ q, highlight, expandAll }: Props)`。
在「原帖答案要点」`</details>` 之后、`帖子出处` 那个 `<div>` 之前插入：
```tsx
      <AnswerBlock answer={q.answer_ai} expandAll={expandAll} />
```

- [ ] **Step 2: 查看 search page 当前如何渲染卡片**

Run: `cat web/src/app/search/page.tsx`
确认结果列表 map 渲染 `<QuestionCard>` 的位置和持有结果数组的变量名。

- [ ] **Step 3: search page 加「展开所有答案」按钮**

在 `web/src/app/search/page.tsx` 结果列表上方加（变量名按 Step 2 实际所见调整；以下假设结果数组为 `results`）：
- import：`import { useState } from "react";`（若未导入）
- 状态：`const [expandAll, setExpandAll] = useState<boolean | undefined>(undefined);`
- 结果存在且至少一条有 `answer_ai` 时显示按钮：
```tsx
{results && results.some((q) => q.answer_ai) && (
  <button
    onClick={() => setExpandAll((v) => !v)}
    className="font-mono text-[10px] uppercase tracking-widest text-accent-ink hover:underline"
  >
    {expandAll ? "收起所有答案" : "展开所有答案"}
  </button>
)}
```
- 把每个 `<QuestionCard q={...} />` 加上 `expandAll={expandAll}`。

- [ ] **Step 4: typecheck + lint**

Run: `cd web && pnpm typecheck && pnpm lint`
Expected: 无错误。

- [ ] **Step 5: Commit**

```bash
git add web/src/components/question-card.tsx web/src/app/search/page.tsx
git commit -m "feat(web): AI answers in search result cards with expand-all

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 文档更新

**Files:**
- Modify: `CLAUDE.md` (Key CLI commands 区)

**Interfaces:** 无。

- [ ] **Step 1: 加 CLI 文档行**

`CLAUDE.md` 的 Key CLI commands 区，`il aggregate` 行附近加：
```
uv run il answer              # Generate difficulty-adaptive AI answers for questions (offline)
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: document 'il answer' command

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 端到端验证（全部任务后手动跑一次）

1. `uv run il answer --limit 3` → 看 outcome 表 generated≥1（需库里有 done 且高质量的题 + DeepSeek key 可用）。
2. `uv run il serve` + `cd web && pnpm dev` → 首页选公司岗位 → 展开「完整题目清单」→ 点某题「AI 解答」展开看 Markdown；点「展开所有答案」批量展开。
3. `/search` 搜一个词 → 卡片上「AI 解答」可展开；顶部「展开所有答案」可用。
4. `uv run il answer --limit 3` 再跑一次 → 同样的题应走缓存（DeepSeek 不再调用，但仍写库 generated 计数）。

## Self-Review 记录

- **Spec 覆盖**：数据列(T1)、配置(T1)、prompt(T2)、每题独立生成+缓存+并发(T3)、CLI 全量+定向+limit+regenerate(T4)、API 两路由+schema(T5)、AnswerBlock+两接入点+展开全部(T6/7/8)、文档(T9)。全覆盖。
- **占位符**：无 TBD/TODO，每个代码步骤含完整代码。
- **类型一致**：`generate_one`/`run_answers`/`AnswerOutcome` 字段在 T3 定义，T4 消费一致；`answer_ai` 列名贯穿 DB→ORM→SQL→schema→TS type→组件 prop 一致；`AnswerBlock` 的 `answer`/`expandAll` prop 在 T6 定义，T7/T8 消费一致。
- **已知前提**：T1 Step2 的 psql 连接参数、T8 Step3 的 search page 变量名需按实际 `.env`/源码微调（已在步骤内注明）。
